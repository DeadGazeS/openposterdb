test-report/
.env
.idea/

# local scratch
.opencode/
.cortexkit/

# agent rules/notes — workspace artifacts, never committed
/RULES.md
/NOTES.md

# scripts/*.tsv — IMDB datasets downloaded by seed.sh; explicitly
# not committed (per scripts/README.md guidance).
scripts/*.tsv
